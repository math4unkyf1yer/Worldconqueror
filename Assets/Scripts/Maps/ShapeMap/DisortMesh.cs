using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisortMesh : MonoBehaviour
{
    public float noiseScale = 3f;
    public float noiseStrenght = 0.3f;
    // Start is called before the first frame update
    void Start()
    {
        ChangeMesh();
    }

    void ChangeMesh()
    {

    }
}
